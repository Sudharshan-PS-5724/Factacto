export const metadata = {
  title: 'Monthly report',
  description: 'Generate and download monthly faculty activity DOCX reports from FACTACTO.',
};

export default function AdminReportsLayout({ children }) {
  return children;
}
