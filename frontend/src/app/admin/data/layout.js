export const metadata = {
  title: 'User data',
  description: 'Browse saved faculty activity submissions by category in FACTACTO admin.',
};

export default function AdminDataLayout({ children }) {
  return children;
}
