export const metadata = {
  title: 'Sign in',
  description:
    'Sign in to FACTACTO — submit faculty activities, open your dashboard, or access admin tools for the IT Department.',
};

export default function LoginLayout({ children }) {
  return children;
}
